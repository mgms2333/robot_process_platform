# 项目概览

## 系统定位

本项目是一套通用机器人工艺控制平台。

平台位于工艺模板和具体机器人、相机、IO 等设备之间，负责把上层工艺意图转换为统一任务模型，并通过运行时执行链驱动设备执行。

平台不绑定具体机器人品牌，也不绑定单一工艺场景。

## 核心职责

- 管理工艺模板插件。
- 根据模板和输入上下文生成统一 `Task`。
- 保存、加载、删除任务实例。
- 查询当前已加载模板列表。
- 通过 `RuntimeService + TaskRunner` 按统一运行时模型执行 `Task`。
- 维护执行状态、执行位置和错误信息。
- 通过统一设备接口调用机器人、相机和其他外设。

## 非职责

- 不实现机器人底层伺服控制。
- 不实现机器人运动插补算法。
- 不把码垛、焊接、上下料等工艺细节写入平台主体。
- 不让模板直接控制机器人或设备。
- 不让模板承担安全、报警、状态机和恢复等平台能力。

## 分层结构

- `core`：定义通用任务数据模型。
- `plugin`：定义模板接口和模板输入输出类型。
- `platform`：提供模板管理、任务管理、日志等平台服务。
- `runtime`：执行生成后的 `Task`。
- `device`：定义机器人、相机等设备抽象接口和设备实现。
- `plugins`：放置具体工艺模板。
- `apps`：放置宿主程序或调试入口。

当前整体架构可表示为：

```text
┌──────────────────────────────────────────────────────┐
│ 1. HMI / 外部入口层                                  │
│ HMI / Runner / 未来 REST API / MES / WCS            │
│                                                      │
│ 负责：                                               │
│ - 接收用户操作                                       │
│ - 选择模板                                           │
│ - 提交任务上下文 JSON                                │
│ - 选择 robot / camera                                │
│ - 发起连接、启动、暂停、停止                         │
│ - 显示运行状态、报警、任务进度                       │
└──────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────┐
│ 2. 平台协调层                                        │
│ PlatformService / SystemController（待落地）         │
│                                                      │
│ 负责：                                               │
│ - 协调 TemplateManager                               │
│ - 协调 TaskManager                                   │
│ - 协调 DeviceManager                                 │
│ - 协调 RuntimeService                                │
│ - 组织“选设备 -> 连接 -> 创建任务 -> 启动执行”流程   │
└──────────────────────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ 3. 模板管理层    │ │ 4. 任务管理层    │ │ 5. 设备管理层    │
│ TemplateManager  │ │ TaskManager      │ │ DeviceManager    │
│                  │ │                  │ │                  │
│ 负责：           │ │ 负责：           │ │ 负责：           │
│ - 加载模板 .so   │ │ - 根据模板生成Task│ │ - 注册 robot     │
│ - 卸载模板       │ │ - 保存 task json │ │ - 注册 camera    │
│ - 创建模板实例   │ │ - 读取 task json │ │ - 选择 active    │
│ - 查询已加载模板 │ │ - 删除 task      │ │   robot/camera   │
└──────────────────┘ └──────────────────┘ └──────────────────┘
          │                    │                    │
          ▼                    ▼                    ▼
┌──────────────────────────────────────────────────────┐
│ 6. 模板插件层                                         │
│ plugins/palletizing/...                               │
│                                                      │
│ 负责：                                               │
│ - 解析输入 JSON                                      │
│ - 生成 ProcessPlan                                   │
│ - Build 成 Task / ExecutionBlock / Action            │
└──────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────┐
│ 7. 运行时服务层                                      │
│ RuntimeService                                       │
│                                                      │
│ 负责：                                               │
│ - 常驻线程                                           │
│ - 命令队列                                           │
│ - 串行处理 Load/Start/Pause/Stop 等命令             │
│ - 持有当前 Task 和 ExecutionContext                  │
│ - 周期性调用 TaskRunner::Tick()                      │
└──────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────┐
│ 8. 运行时执行层                                      │
│ TaskRunner / StateMachine / Scheduler / Queue        │
│ BlockExecutor / ExecutionContext                     │
│                                                      │
│ 负责：                                               │
│ - 装载 Task                                          │
│ - 状态迁移                                           │
│ - 按 Action 推进执行                                 │
│ - 记录 block/action 位置                             │
│ - 调用统一设备接口                                   │
└──────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────┐
│ 9. 设备抽象层                                        │
│ IRobot / ICamera                                     │
│                                                      │
│ 负责：                                               │
│ - 给 runtime 提供统一设备能力                        │
│ - 屏蔽不同厂家 SDK 差异                              │
└──────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────┐
│ 10. 设备实现 / 适配层                                │
│ HyRobot / 未来 Camera 实现 / 其他厂家 Robot Adapter │
│                                                      │
│ 负责：                                               │
│ - mock 执行                                          │
│ - 或调用真实厂家 SDK                                 │
│ - Connect / Disconnect / 状态查询                    │
│ - 参数翻译、IO 映射                                  │
└──────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────┐
│ 11. 厂家 SDK / 控制器 / 真设备                       │
│ HR_Pro SDK / Robot Controller / Camera SDK          │
└──────────────────────────────────────────────────────┘
```

如果只看主干关系，可以进一步压缩为：

```text
HMI / Runner
    │
    ▼
Platform Layer
(TemplateManager / TaskManager / DeviceManager / 未来 PlatformService)
    │
    ├── 模板 -> 生成 Task
    │
    └── 设备 -> 提供 active robot / camera
    ▼
Runtime Layer
(RuntimeService -> TaskRunner -> BlockExecutor)
    ▼
Device Abstraction
(IRobot / ICamera)
    ▼
Concrete Device
(HyRobot / Camera Adapter / SDK)
```

## 统一任务模型

平台执行端统一消费以下模型：

```text
Task
    |
    v
ExecutionBlock
    |
    v
Action
```

含义如下：

- `Task` 表示一次完整任务实例。
- `ExecutionBlock` 表示一个可调度的连续动作块。
- `Action` 表示运行时执行器直接消费的最小执行单元。

模板可以有自己的工艺语义对象，例如码垛模板中的箱子、层、垛型和工艺块，但模板输出必须落到统一 `Task` 模型。

## 标准流程

```text
输入工艺上下文 JSON
    |
    v
TemplateManager 加载模板
    |
    v
TaskManager 调用模板创建 Task
    |
    v
TaskManager 保存 Task / 读取 Task
    |
    v
RuntimeService 装载 Task
    |
    v
RuntimeService 驱动 TaskRunner 按 Action 推进执行
    |
    v
Device 接口驱动机器人或外设
```

## 当前设备接入形态

当前项目中，运行时通过统一 `IRobot` 接口驱动机器人。

- `TaskRunner / BlockExecutor` 只调用统一动作接口。
- `HyRobot` 是当前默认机器人实现。
- `HyRobot` 默认以 mock 模式运行，用于验证平台主链。
- 当 CMake 打开华沿 SDK 支持后，`HyRobot` 可切换到真实华沿 SDK 适配分支。
- 机器人连接动作属于平台或设备准备流程，不属于 runtime 隐式执行流程。
